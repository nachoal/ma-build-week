# Realtime session broker

This directory will contain the smallest Cloudflare Worker needed to mint
short-lived Realtime client secrets and run the bounded post-lesson planner.
The standard OpenAI API key remains on the server. Do not introduce a broader
backend before it is needed.
